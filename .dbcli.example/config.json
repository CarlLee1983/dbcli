{
  "connection": {
    "system": "postgresql",
    "host": {
      "$env": "DB_HOST"
    },
    "port": {
      "$env": "DB_PORT"
    },
    "user": {
      "$env": "DB_USER"
    },
    "password": {
      "$env": "DB_PASSWORD"
    },
    "database": {
      "$env": "DB_DATABASE"
    }
  },
  "permission": "query-only",
  "schema": {},
  "metadata": {
    "version": "1.0"
  }
}
