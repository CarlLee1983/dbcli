---
phase: 07-data-modification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/data.ts
  - src/core/data-executor.ts
  - src/commands/insert.ts
  - src/cli.ts
  - tests/unit/core/data-executor.test.ts
  - tests/unit/commands/insert.test.ts
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - "User can insert a single row via JSON stdin with --force flag"
    - "INSERT command displays generated SQL before execution"
    - "INSERT requires Read-Write or Admin permission (Query-only rejects)"
    - "Query-only mode returns permission error with upgrade suggestion"
    - "--dry-run shows SQL without executing"
    - "Parameterized queries prevent SQL injection"
    - "Output is JSON format: {status, rows_affected, operation}"
  artifacts:
    - path: "src/types/data.ts"
      provides: "Type definitions for data modification"
      exports: ["DataExecutionResult", "DataExecutionOptions"]
    - path: "src/core/data-executor.ts"
      provides: "DataExecutor class with INSERT execution and permission checks"
      exports: ["DataExecutor"]
    - path: "src/commands/insert.ts"
      provides: "INSERT command handler with stdin/--data flag parsing"
      exports: ["insertCommand"]
  key_links:
    - from: "src/commands/insert.ts"
      to: "src/core/data-executor.ts"
      via: "instantiate DataExecutor for execution"
      pattern: "new DataExecutor\\(adapter, config.permission\\)"
    - from: "src/core/data-executor.ts"
      to: "src/adapters/types.ts"
      via: "call adapter.execute() with parameterized queries"
      pattern: "this.adapter.execute\\(sql, params\\)"
    - from: "src/core/data-executor.ts"
      to: "src/core/permission-guard.ts"
      via: "enforce permission via enforcePermission()"
      pattern: "enforcePermission\\('INSERT'"
---

<objective>
Implement core data modification infrastructure and `dbcli insert` command with comprehensive safety safeguards, permission checks, pre-execution confirmation, and SQL injection prevention.

Purpose: Enable read-write AI agents to insert data into databases with clear visual feedback, mandatory confirmation, and safe parameterized queries.

Output:
- DataExecutionResult and DataExecutionOptions type definitions
- DataExecutor class for INSERT execution with permission enforcement
- INSERT command registered in CLI
- Fully tested with 35+ unit tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-data-modification/07-CONTEXT.md

# Key decision references
# D-01: JSON stdin primary input method
# D-03a: Show SQL + interactive y/n confirmation
# D-03b: --force flag to skip confirmation
# D-03c: --dry-run mode to show SQL without executing
# D-04a: Output: simple summary (row count only)
# D-04b: Output format: JSON
# D-05a: Show SQL in error messages
# D-05b: Permission rejection: clear message + upgrade suggestion
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data types module (src/types/data.ts)</name>
  <files>src/types/data.ts, src/types/index.ts</files>
  <read_first>
    - src/types/query.ts (QueryResult interface pattern to follow)
    - src/types/index.ts (export structure)
    - src/core/permission-guard.ts (PermissionError to understand)
  </read_first>
  <action>
Create `src/types/data.ts` with TypeScript interfaces for data modification operations:

1. **DataExecutionResult interface:**
   - `status: 'success' | 'error'`
   - `operation: 'insert' | 'update'`
   - `rows_affected: number`
   - `timestamp?: string` (ISO 8601)
   - `sql?: string` (generated SQL shown in results)
   - `error?: string` (only if status is 'error')

2. **DataExecutionOptions interface:**
   - `dryRun?: boolean`
   - `force?: boolean`
   - `verbose?: boolean`

Export all types from `src/types/index.ts`.

Implementation notes:
- Align with existing QueryResult interface pattern
- Keep types focused and minimal
  </action>
  <verify>
    <automated>grep -n "export interface DataExecutionResult\|export interface DataExecutionOptions" src/types/data.ts</automated>
  </verify>
  <done>
- File src/types/data.ts exists with exported interfaces
- Both interfaces properly typed with required fields
- Exported from src/types/index.ts
- TypeScript compilation successful
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement DataExecutor class (src/core/data-executor.ts)</name>
  <files>src/core/data-executor.ts</files>
  <read_first>
    - src/core/query-executor.ts (QueryExecutor pattern to follow)
    - src/core/permission-guard.ts (enforcePermission function)
    - src/adapters/types.ts (DatabaseAdapter interface)
    - src/adapters/postgresql.ts (parameterized query examples with $1, $2)
    - src/adapters/mysql.ts (parameterized query examples with ?)
  </read_first>
  <action>
Create `src/core/data-executor.ts` with DataExecutor class for INSERT execution:

**Constructor:**
```typescript
constructor(
  private adapter: DatabaseAdapter,
  private permission: Permission
)
```

**Key Methods:**

1. **buildInsertSql(tableName: string, data: Record<string, any>, schema: TableSchema): { sql: string, params: any[] }**
   - Validate all fields exist in table schema
   - Generate parameterized INSERT with $1, $2 (PostgreSQL) or ? (MySQL)
   - Return {sql, params}

2. **async executeInsert(tableName: string, data: Record<string, any>, schema: TableSchema, options?: DataExecutionOptions): Promise<DataExecutionResult>**
   - Call `enforcePermission('INSERT', this.permission)` — throw PermissionError if rejected
   - Build SQL via buildInsertSql()
   - If dryRun: return result with rows_affected=0
   - If not force: call confirmation prompt
   - Execute via adapter.execute(sql, params)
   - Return DataExecutionResult with rows_affected

**Error Handling:**
- Catch PermissionError with message: "Permission denied: Query-only mode allows SELECT only. Use Read-Write or Admin mode for INSERT."
- Include generated SQL in error messages (per D-05a)
  </action>
  <verify>
    <automated>grep -n "export class DataExecutor\|async executeInsert\|buildInsertSql" src/core/data-executor.ts</automated>
  </verify>
  <done>
- File src/core/data-executor.ts exists with DataExecutor class
- Constructor accepts DatabaseAdapter and Permission
- executeInsert() method implemented with all steps
- buildInsertSql() generates proper parameterized SQL
- Permission enforcement via enforcePermission()
- Error messages include generated SQL
- TypeScript compilation successful (0 errors)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create insert command handler (src/commands/insert.ts)</name>
  <files>src/commands/insert.ts</files>
  <read_first>
    - src/commands/query.ts (command handler structure and error handling pattern)
    - src/commands/init.ts (option parsing and validation patterns)
    - src/core/config.ts (config reading pattern)
    - src/adapters/factory.ts (AdapterFactory usage)
  </read_first>
  <action>
Create `src/commands/insert.ts` with insert command handler following query.ts pattern:

**Function signature:**
```typescript
export async function insertCommand(
  table: string,
  options: {
    data?: string
    dryRun?: boolean
    force?: boolean
  }
): Promise<void>
```

**Implementation steps:**

1. Validate table name is not empty
2. Parse JSON from stdin or --data flag
3. Load `.dbcli` config file
4. Create database adapter via AdapterFactory
5. Get table schema via adapter.getTableSchema()
6. Create DataExecutor and call executeInsert()
7. Format output as JSON and console.log()
8. Handle errors: PermissionError, ConnectionError, ValidationError

**Error handling chain:**
- PermissionError: Display "Permission Denied" with operation type
- ConnectionError: Display "Database Connection Failed"
- ValidationError: Display "Insert Error" with message
- All errors: exit code 1

**Example usage:**
```bash
echo '{"name":"Alice","email":"a@b.com"}' | dbcli insert users
dbcli insert users --data '{"name":"Alice"}' --force
dbcli insert users --data '{"name":"Alice"}' --dry-run
```
  </action>
  <verify>
    <automated>grep -n "export async function insertCommand" src/commands/insert.ts</automated>
  </verify>
  <done>
- File src/commands/insert.ts exists with insertCommand function
- Accepts table, data, dryRun, force parameters
- Reads from stdin or --data flag
- Parses JSON with error handling
- Loads config and creates adapter
- Calls DataExecutor.executeInsert()
- Formats output as JSON
- Handles all error cases
- TypeScript compilation successful
  </done>
</task>

<task type="auto">
  <name>Task 4: Register insert command in CLI (src/cli.ts)</name>
  <files>src/cli.ts</files>
  <read_first>
    - src/cli.ts (current command registration structure)
    - src/commands/query.ts (command registration example from query command)
  </read_first>
  <action>
Register INSERT command in src/cli.ts:

**Steps:**

1. Import insertCommand at top
2. Add command registration before .parse():
   ```typescript
   program
     .command('insert <table>')
     .description('Insert data into database table')
     .option('--data <json>', 'JSON object to insert')
     .option('--dry-run', 'Show generated SQL without executing')
     .option('--force', 'Skip confirmation prompt')
     .action(async (table: string, options: any) => {
       try {
         await insertCommand(table, options)
       } catch (error) {
         console.error((error as Error).message)
         process.exit(1)
       }
     })
   ```

3. Place registration after query command for consistency
4. Verify help text displays correctly via: `dbcli insert --help`
  </action>
  <verify>
    <automated>grep -n "command('insert'" src/cli.ts</automated>
  </verify>
  <done>
- insertCommand imported at top of src/cli.ts
- Command registered with .command('insert <table>')
- All flags (--data, --dry-run, --force) specified
- Action handler calls insertCommand() with proper error handling
- Help text accessible via dbcli insert --help
  </done>
</task>

<task type="auto">
  <name>Task 5: Write comprehensive unit tests (tests/unit/core/data-executor.test.ts, tests/unit/commands/insert.test.ts)</name>
  <files>tests/unit/core/data-executor.test.ts, tests/unit/commands/insert.test.ts</files>
  <read_first>
    - tests/unit/core/query-executor.test.ts (test structure and mock patterns)
    - tests/unit/commands/query.test.ts (command test patterns)
    - src/core/permission-guard.ts (PermissionError class)
  </read_first>
  <action>
Create two comprehensive test files:

**tests/unit/core/data-executor.test.ts (20+ tests):**

1. buildInsertSql() Tests (6 tests):
   - Simple INSERT with 2 columns
   - NULL values handling
   - Missing column validation
   - Parameterization: $1, $2 vs ?
   - Data type preservation
   - Column order consistency

2. executeInsert() Permission Tests (4 tests):
   - Query-only rejects INSERT
   - Read-Write allows INSERT
   - Admin allows INSERT
   - Error message includes upgrade suggestion

3. executeInsert() Execution Tests (5 tests):
   - Successful INSERT with 1 row
   - --dry-run mode returns rows_affected=0
   - --force mode skips confirmation
   - Confirmation flow works
   - Error messages include SQL

4. Error Handling Tests (5 tests):
   - Database error includes SQL
   - Connection error propagates
   - Invalid table error
   - Column not found error
   - NULL parameter handling

**tests/unit/commands/insert.test.ts (15+ tests):**

1. Argument Validation (3 tests):
   - Reject missing table name
   - Accept table name
   - Valid JSON from --data flag

2. Data Input Tests (4 tests):
   - Valid JSON parsing
   - Invalid JSON error handling
   - Empty JSON object handling
   - Missing data error

3. Permission Enforcement (4 tests):
   - Query-only rejects INSERT
   - Read-Write allows INSERT
   - Admin allows INSERT
   - Error message format

4. Execution Options (2 tests):
   - --dry-run mode
   - --force mode

5. Output Formatting (2 tests):
   - JSON output format
   - Error output format

**Mock Strategy:**
- Mock DatabaseAdapter.execute() returning row count
- Mock enforcePermission() for permission tests
- Mock promptConfirmation() for confirmation tests
- Use jest.fn() or Bun test equivalent for tracking

**Coverage Target:** 80%+ for both files
  </action>
  <verify>
    <automated>grep -n "test\|describe" tests/unit/core/data-executor.test.ts tests/unit/commands/insert.test.ts | wc -l</automated>
  </verify>
  <done>
- Both test files exist with 35+ total tests
- Tests cover buildInsertSql, executeInsert, insertCommand
- Tests for permissions, execution, errors, output
- All tests pass: `bun test --run`
- Coverage at or above 80%
- Proper mock setup for DatabaseAdapter and enforcePermission
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. **DataExecutor Class Functionality:**
   - buildInsertSql() generates proper parameterized SQL
   - executeInsert() enforces permissions correctly
   - --dry-run and --force modes work
   - Confirmation prompt works

2. **INSERT Command Functionality:**
   - `echo '{"name":"Alice"}' | dbcli insert users` works
   - `dbcli insert users --data '{"name":"Alice"}'` works as alternative
   - Query-only mode rejects insert with permission message
   - --dry-run shows SQL without executing
   - --force skips confirmation

3. **Permission Enforcement:**
   - Query-only: clear error with upgrade suggestion
   - Read-Write: allows INSERT
   - Admin: allows INSERT

4. **Output Formatting:**
   - JSON: {status, operation, rows_affected, timestamp}
   - Errors: include generated SQL

5. **SQL Injection Prevention:**
   - Parameterized queries properly used
   - adapter.execute(sql, params) called correctly

6. **Test Coverage:**
   - 35+ unit tests, all passing
   - Zero regressions from Phase 6
</verification>

<success_criteria>
- [x] INSERT command works with proper flags
- [x] JSON stdin and --data flag both work
- [x] Pre-execution SQL display + y/n confirmation (default) works
- [x] --force skips confirmation
- [x] --dry-run shows SQL without executing
- [x] Permission levels enforced (Query-only rejects, Read-Write allows)
- [x] Parameterized queries prevent SQL injection
- [x] Output is JSON format with row count
- [x] Errors show generated SQL + helpful messages
- [x] All 35+ tests pass
- [x] No regressions in existing code
</success_criteria>

<output>
After completion, create `.planning/phases/07-data-modification/07-01-SUMMARY.md` with:
- List of completed tasks with commit hashes
- Implementation details for DataExecutor class
- INSERT command implementation details
- Test results and coverage metrics
- Build verification results
</output>
